version: "3.9"

services:
  todo-app:
    build: .
    container_name: todo-container

    ports:
      - "3000:3000"

    restart: unless-stopped

    volumes:
      - ./app/data:/app/data

    environment:
      NODE_ENV: production